sources:

  - name: source1
    type: database
    config:
      host: localhost
      port: 5432
      database: sample_db
      username: db_user
      password: db_password

  - name: source2
    type: prometheus
    config:
      endpoint: http://prometheus-server:9090