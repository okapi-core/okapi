apiVersion: v1
kind: Service
metadata:
  name: {{ include "okapi-ingester.fullname" . }}
  labels:
    {{- include "okapi-ingester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "okapi-ingester.selectorLabels" . | nindent 4 }}
