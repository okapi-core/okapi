apiVersion: v1
kind: Service
metadata:
  name: {{ include "okapi-web.fullname" . }}
  labels:
    {{- include "okapi-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "okapi-web.selectorLabels" . | nindent 4 }}
